import React from 'react';
import { createRoot } from 'react-dom/client';

// WP4: replace this placeholder with the real app (router, pages, etc.)
function App() {
  return <div>Diun Updater — loading…</div>;
}

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
